#include <stdio.h>

void cube(float *ptr);

int main(void)
{
    float x = 0.0f;
    
    printf("> ");
    scanf("%f", &x);
    
    cube(&x);
    
    printf("x holds %f\n", x);
    
    return 0;
}

void cube(float *ptr)
{
    *ptr = (*ptr) * (*ptr) * (*ptr);
}